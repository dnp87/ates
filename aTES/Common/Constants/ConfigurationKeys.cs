namespace Common.Constants
{
    public static class ConfigurationKeys
    {
        public const string KafkaBootstrapServers = nameof(KafkaBootstrapServers);
        public const string ConsumerGroupId = nameof(ConsumerGroupId);
        public const string AuthenticationUrl = nameof(AuthenticationUrl);
    }
}
